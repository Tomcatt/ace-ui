[project]
name = "ace-ui"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
]

[project.scripts]
ace-ui = "server:main"

[dependency-groups]
dev = [
    "playwright>=1.60.0",
]
